{% include 'header.html.twig' %}    {# JE FAIS UN INCLUDE DE MON HEADER POUR L'AFFICHER  #}


    <div class="article">
        {% for article in article %}    {# BOUCLE POUR RECUPERER TOUT LES ARTICLES DE MON ARRAY#}
            <div class="article_2">
                <img src="{{ article.image }}" alt="" class="img_article">  {# DECLARATION DE LA VARIABLE "ARTICLE.IMAGE" POUR RECUPERER LES IMAGES DE MON ARRAY#}
                <p>{{ article.title }}</p>  {# DECLARATION DE LA VARIABLE "ARTICLE.TITLE" POUR RECUPERER LES TITRES DE MON ARRAY#}
            </div>
        {% endfor %}
    </div>


{% include 'footer.html.twig' %}    {# JE FAIS UN INCLUDE DE MON FOOTER POUR L'AFFICHER  #}